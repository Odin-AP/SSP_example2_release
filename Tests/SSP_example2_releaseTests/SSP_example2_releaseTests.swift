import XCTest
@testable import SSP_example2_release

final class SSP_example2_releaseTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SSP_example2_release().text, "Hello, World!")
    }
}
